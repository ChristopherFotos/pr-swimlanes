{
  "name": "pr-swimlanes-sqlite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "pm2 start src/server.js --name pr_swimlanes",
    "dev": "nodemon --watch src --ext js,css,html src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
